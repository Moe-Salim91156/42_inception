[mysqld]
# ⚠️  Must bind to 0.0.0.0 so WordPress container can connect
#     Default is 127.0.0.1 which would refuse connections from other containers
bind-address    = 0.0.0.0
port            = 3306

# Performance tuning (optional but good practice)
skip-name-resolve
max_connections = 100
